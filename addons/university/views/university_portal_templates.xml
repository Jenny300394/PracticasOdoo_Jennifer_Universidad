<odoo>
    <template id="portal_my_home_menu_grades" inherit_id="portal.portal_my_home" priority="30">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="inside">
            <t t-if="grade_count is not False">
                <a class="list-group-item list-group-item-action d-flex align-items-center justify-content-between" 
                   href="/my/grades" 
                   title="Grades">
                    <div class="d-flex align-items-center">
                        <i class="fa fa-graduation-cap fa-2x fa-fw text-primary me-3"/>
                        <span>Calificaciones</span>
                    </div>
                    </a>
            </t>
        </xpath>
    </template>

    <template id="portal_my_grades_list" name="My Grades">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>
            <div class="container mt-3">
                <h3 class="mb-4">Academic Grades History</h3>
                
                <t t-if="not grades">
                    <div class="alert alert-info">Aún no tienes calificaciones registradas.</div>
                </t>

                <table t-if="grades" class="table table-hover bg-white o_portal_my_doc_table">
                    <thead>
                        <tr class="active">
                            <th>Subject</th>
                            <th>Professor</th>
                            <th>Date</th>
                            <th class="text-center">Grade</th>
                        </tr>
                    </thead>
                    <tbody>
                        <t t-foreach="grades" t-as="g">
                            <tr>
                                <td>
                                    <span t-if="g.enrollment_id" t-out="g.enrollment_id.subject_id.name"/>
                                </td>
                                <td>
                                    <span t-if="g.enrollment_id" t-out="g.enrollment_id.professor_id.name"/>
                                </td>
                                <td>
                                    <span t-field="g.create_date" t-options='{"widget": "date"}'/>
                                </td>
                                <td class="text-center">
                                    <span t-attf-class="badge rounded-pill #{'text-bg-success' if g.grade >= 5 else 'text-bg-danger'}"
                                          t-out="g.grade"/>
                                </td>
                            </tr>
                        </t>
                    </tbody>
                </table>
            </div>
        </t>
    </template>
</odoo>